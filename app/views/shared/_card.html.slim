= link_to spot_path(spot), class: "Card" do
  .Card__inner
    figure.Card__img
      - if spot.spots_photos.present?
        img class="lazyload" alt="#{spot.name}" src="#{spot.spots_photos.first.photo}"
    .Card__body
      h3.Card__title = spot.name
      .Card__address = spot.address
      .Card__meta
        .Card__rating
          = render "shared/card_stars", rating: rating(spot), count: reviews_count(spot)
        = render "shared/heart", spot: spot, favorite: favorite
